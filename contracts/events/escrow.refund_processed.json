{
  "event_type": "escrow.refund_processed",
  "version": "v1",
  "producer": "M13-Escrow-Ledger-Service",
  "payload_schema": {
    "type": "object",
    "required": ["escrow_id", "amount", "refunded_at"],
    "properties": {
      "escrow_id": { "type": "string" },
      "amount": { "type": "number" },
      "refunded_at": { "type": "string", "format": "date-time" }
    }
  }
}
