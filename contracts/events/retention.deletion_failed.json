{
  "event_type": "retention.deletion_failed",
  "version": "v1",
  "producer": "M68-Retention-Service",
  "event_class": "domain",
  "partition_key_path": "data.entity_id",
  "payload_schema": {
    "type": "object",
    "required": ["deletion_id", "policy_id", "entity_id", "data_type", "error_message", "failed_at"],
    "properties": {
      "deletion_id": { "type": "string" },
      "policy_id": { "type": "string" },
      "entity_id": { "type": "string" },
      "data_type": { "type": "string" },
      "error_message": { "type": "string" },
      "failed_at": { "type": "string", "format": "date-time" }
    }
  }
}
