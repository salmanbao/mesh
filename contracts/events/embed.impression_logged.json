{
  "event_type": "embed.impression_logged",
  "version": "v1",
  "producer": "M66-Embed-Service",
  "event_class": "ops",
  "partition_key_path": "envelope.source_service",
  "payload_schema": {
    "type": "object",
    "required": ["entity_type", "entity_id"],
    "properties": {
      "entity_type": { "type": "string", "enum": ["whop", "campaign", "app", "clip"] },
      "entity_id": { "type": "string" },
      "referrer_domain": { "type": "string" },
      "user_agent_browser": { "type": "string" },
      "dnt_enabled": { "type": "boolean" },
      "ip_anonymized": { "type": "string" }
    }
  }
}
