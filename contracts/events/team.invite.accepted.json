{
  "event_type": "team.invite.accepted",
  "version": "v1",
  "producer": "M97-Team-Service",
  "payload_schema": {
    "type": "object",
    "required": ["team_id", "invite_id", "user_id", "accepted_at"],
    "properties": {
      "team_id": { "type": "string" },
      "invite_id": { "type": "string" },
      "user_id": { "type": "string" },
      "accepted_at": { "type": "string", "format": "date-time" }
    }
  }
}
