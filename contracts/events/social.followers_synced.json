{
  "event_type": "social.followers_synced",
  "version": "v1",
  "producer": "M10-Social-Integration-Verification-Service",
  "payload_schema": {
    "type": "object",
    "required": ["user_id", "platform", "follower_count", "synced_at"],
    "properties": {
      "user_id": { "type": "string" },
      "platform": { "type": "string" },
      "follower_count": { "type": "integer" },
      "synced_at": { "type": "string", "format": "date-time" }
    }
  }
}
