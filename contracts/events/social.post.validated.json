{
  "event_type": "social.post.validated",
  "version": "v1",
  "producer": "M10-Social-Integration-Verification-Service",
  "payload_schema": {
    "type": "object",
    "required": ["user_id", "platform", "post_id", "validated_at"],
    "properties": {
      "user_id": { "type": "string" },
      "platform": { "type": "string" },
      "post_id": { "type": "string" },
      "validated_at": { "type": "string", "format": "date-time" }
    }
  }
}
