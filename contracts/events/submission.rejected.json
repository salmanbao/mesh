{
  "event_type": "submission.rejected",
  "version": "v1",
  "producer": "M26-Submission-Service",
  "payload_schema": {
    "type": "object",
    "required": [
      "submission_id",
      "user_id",
      "campaign_id",
      "reason",
      "rejected_at"
    ],
    "properties": {
      "submission_id": {
        "type": "string"
      },
      "user_id": {
        "type": "string"
      },
      "campaign_id": {
        "type": "string"
      },
      "reason": {
        "type": "string"
      },
      "rejected_at": {
        "type": "string",
        "format": "date-time"
      }
    }
  }
}
