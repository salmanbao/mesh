{
  "event_type": "user.deleted",
  "version": "v1",
  "producer": "M01-Authentication-Service",
  "payload_schema": {
    "type": "object",
    "required": [
      "user_id",
      "deleted_at"
    ],
    "properties": {
      "user_id": {
        "type": "string",
        "format": "uuid"
      },
      "deleted_at": {
        "type": "string",
        "format": "date-time"
      }
    }
  }
}
