{
  "event_type": "team.member.added",
  "version": "v1",
  "producer": "M97-Team-Service",
  "payload_schema": {
    "type": "object",
    "required": ["team_id", "user_id", "role", "added_at"],
    "properties": {
      "team_id": { "type": "string" },
      "user_id": { "type": "string" },
      "role": { "type": "string" },
      "added_at": { "type": "string", "format": "date-time" }
    }
  }
}
