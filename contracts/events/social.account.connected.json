{
  "event_type": "social.account.connected",
  "version": "v1",
  "producer": "M10-Social-Integration-Verification-Service",
  "payload_schema": {
    "type": "object",
    "required": ["user_id", "platform", "connected_at"],
    "properties": {
      "user_id": { "type": "string" },
      "platform": { "type": "string" },
      "connected_at": { "type": "string", "format": "date-time" }
    }
  }
}
