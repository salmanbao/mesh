{
  "event_type": "retention.legal_hold_released",
  "version": "v1",
  "producer": "M68-Retention-Service",
  "event_class": "domain",
  "partition_key_path": "data.entity_id",
  "payload_schema": {
    "type": "object",
    "required": ["hold_id", "entity_id", "released_at", "status"],
    "properties": {
      "hold_id": { "type": "string" },
      "entity_id": { "type": "string" },
      "released_at": { "type": "string", "format": "date-time" },
      "status": { "type": "string" },
      "released_by": { "type": "string" }
    }
  }
}
