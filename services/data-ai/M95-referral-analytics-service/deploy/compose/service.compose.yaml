services:
  m95-referral-analytics-service:
    build:
      context: ../../services/data-ai/M95-referral-analytics-service
      dockerfile: Dockerfile
    env_file:
      - ../../.env.example
    networks: [mesh]
networks:
  mesh: {}
