services:
  m25-auto-clipping-ai:
    build:
      context: ../../services/data-ai/M25-auto-clipping-ai
      dockerfile: Dockerfile
    env_file:
      - ../../.env.example
    networks: [mesh]
networks:
  mesh: {}
