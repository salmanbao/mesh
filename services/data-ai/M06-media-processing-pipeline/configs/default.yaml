service:
  id: M06-Media-Processing-Pipeline
  cluster: data-ai
  http_port: 8080
  grpc_port: 9090

dependencies:
  campaign_grpc_url: ${CAMPAIGN_GRPC_URL}

pipeline:
  idempotency_ttl_hours: 168
  event_dedup_ttl_hours: 168
  worker_poll_seconds: 2
