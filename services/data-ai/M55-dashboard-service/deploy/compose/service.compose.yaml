services:
  m55-dashboard-service:
    build:
      context: ../../services/data-ai/M55-dashboard-service
      dockerfile: Dockerfile
    env_file:
      - ../../.env.example
    networks: [mesh]
networks:
  mesh: {}
