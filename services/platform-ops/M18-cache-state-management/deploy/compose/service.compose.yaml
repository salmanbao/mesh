services:
  m18-cache-state-management:
    build:
      context: ../../services/platform-ops/M18-cache-state-management
      dockerfile: Dockerfile
    env_file:
      - ../../.env.example
    networks: [mesh]
networks:
  mesh: {}
