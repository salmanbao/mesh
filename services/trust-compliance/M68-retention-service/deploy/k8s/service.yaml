apiVersion: v1
kind: Service
metadata:
  name: m68-retention-service
spec:
  selector:
    app: m68-retention-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
    - name: grpc
      port: 9090
      targetPort: 9090

