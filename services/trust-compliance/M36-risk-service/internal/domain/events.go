package domain

// M36 currently has no canonical event deps/provides in dependencies.yaml.
// Spec Kafka topics are module-internal (`seller.*`) and are intentionally not emitted as cross-service contracts here.
