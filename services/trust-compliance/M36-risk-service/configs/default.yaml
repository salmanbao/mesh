service:
  id: M36-Risk-Service
  cluster: trust-compliance
  http_port: 8080
  grpc_port: 9090
dependencies:
  auth_grpc_url: ${AUTH_GRPC_URL}
  profile_grpc_url: ${PROFILE_GRPC_URL}
  fraud_grpc_url: ${FRAUD_GRPC_URL}
  moderation_grpc_url: ${MODERATION_GRPC_URL}
  resolution_grpc_url: ${RESOLUTION_GRPC_URL}
  reputation_grpc_url: ${REPUTATION_GRPC_URL}
security:
  webhook_bearer_token: ${RISK_WEBHOOK_BEARER_TOKEN}
observability:
  otlp_endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT}
