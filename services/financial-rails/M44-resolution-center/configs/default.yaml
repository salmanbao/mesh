service:
  id: M44-Resolution-Center
  cluster: financial-rails
  http_port: 8080
  grpc_port: 9090
dependencies:
  moderation_grpc_url: ${MODERATION_GRPC_URL}
observability:
  otlp_endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT}
