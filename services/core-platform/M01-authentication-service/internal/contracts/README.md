# Internal Contracts

This directory contains service-local wire contracts used by adapters.

- Public cross-service contracts live under `mesh/contracts/*`.
- Service-local contracts should remain stable and avoid leaking adapter-specific fields.
